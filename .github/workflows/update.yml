name: Actualizar Telefe M3U

on:
  schedule:
    - cron: "0 */4 * * *"   # Cada 4 horas
  workflow_dispatch:        # Permite ejecutarlo manualmente

jobs:
  actualizar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Instalar dependencias
        run: pip install requests

      - name: Ejecutar script
        run: python telefe.py

      - name: Commit y push
        run: |
          git config --global user.name "fakuz"
          git config --global user.email "fakuz@users.noreply.github.com"
          git add telefe.m3u
          git commit -m "🔄 Actualización automática Telefe.m3u" || echo "Sin cambios"
          git push